# Ocean Master - Code Runner

## Setup
- `git submodule init`
- `git submodule update --init --recursive`
- install protoc using `go install` and then `go generate internal/nsjail/config.go`
- `docker compose up --build`

it shud work.
